# Server Configuration
PORT=5000
NODE_ENV=development

# ============================================
# Application Database (MongoDB)
# This stores all app data: users, projects, schemas
# Database: nocodedb_app
# Collections: users, projects, schemas
# ============================================
MONGODB_URI=mongodb+srv://tousif:Tousif3@cluster0.nywis.mongodb.net/nocodedb_app?appName=Cluster0

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# ============================================
# User Database Connections (for schema testing/export)
# These are used when users want to test or deploy their schemas
# ============================================

# Default MySQL Connection (for user schema testing)
USER_MYSQL_HOST=localhost
USER_MYSQL_PORT=3306
USER_MYSQL_USER=root
USER_MYSQL_PASSWORD=
USER_MYSQL_DATABASE=test_db

# Default MongoDB Connection (for user schema testing)
USER_MONGODB_URI=mongodb://localhost:27017
