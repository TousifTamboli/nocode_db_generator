# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration (Application Database)
MONGODB_URI=mongodb://localhost:27017/nocodedb

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# ============================================
# User Database Connection Defaults
# These are used when users want to test/export their schemas
# ============================================

# Default MySQL Connection (for user schema testing)
USER_MYSQL_HOST=localhost
USER_MYSQL_PORT=3306
USER_MYSQL_USER=root
USER_MYSQL_PASSWORD=
USER_MYSQL_DATABASE=test_db

# Default MongoDB Connection (for user schema testing)
USER_MONGODB_URI=mongodb://localhost:27017
